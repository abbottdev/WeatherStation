<UserControl x:Class="WeatherStation.Windows.Views.SelectLocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherStation.Windows.Views"
             xmlns:vm="clr-namespace:WeatherStation.Windows.ViewModels"
             mc:Ignorable="d" 
             x:Name="theView" d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False,Type=vm:SelectLocationViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock>Select Location</TextBlock>
            <TextBlock>Begin typing a location to find it from the dropdown</TextBlock>
            <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <ListBox Height="150" ItemsSource="{Binding SearchResults}" SelectedItem="{Binding SelectedLocation}" IsSynchronizedWithCurrentItem="True" DisplayMemberPath="DisplayName"></ListBox>
            <Button Command="{Binding SaveSelectedLocation}" Content="Save"></Button>
        </StackPanel>
    </Grid>
</UserControl>
